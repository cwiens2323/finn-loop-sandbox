# Agent Rules

This is a private Finn-loop verification repository.

- Read `.finn-loop.yaml` before work.
- Implement only the linked Linear issue contract.
- Run `python -m unittest discover -s tests -v` before opening or updating a PR.
- Never merge or enable auto-merge. Chad must explicitly authorize each final merge.
- Never commit secrets or private family/business data.
